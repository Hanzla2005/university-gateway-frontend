import { useEffect } from "react";

const UniversityAct = () => {
  useEffect(() => {
    // This route handler is not needed as the PDF is opened directly from navbar
    // This page can be extended in the future if needed
  }, []);

  return null;
};

export default UniversityAct;
